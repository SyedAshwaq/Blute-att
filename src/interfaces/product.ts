export interface Product {
    edicode: string;
    id: number;
    lenscode: string;
    productdesc: string;
    }

export interface ProductDetail{
    id: number;
    pdtype: string;
    pdvalue: string
}